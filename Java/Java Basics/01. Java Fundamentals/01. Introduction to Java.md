# ☕ Introduction to Java

## 📖 What is Java?

Java is a high-level, object-oriented, platform-independent programming language developed by James Gosling at Sun Microsystems (now Oracle) in 1995.

**Original Name**: Oak (renamed to Java)

---

## 🎯 Key Features of Java

### 1. **Simple and Easy to Learn**
- Syntax similar to C/C++ but simpler
- No pointers, operator overloading
- Automatic memory management (Garbage Collection)

### 2. **Object-Oriented**
- Everything is an object (except primitives)
- Supports inheritance, polymorphism, encapsulation, abstraction

### 3. **Platform Independent (WORA)**
- **Write Once, Run Anywhere**
- Java code compiles to bytecode
- Bytecode runs on any platform with JVM

```
Source Code (.java) → Compiler → Bytecode (.class) → JVM → Machine Code
```

### 4. **Robust**
- Strong type checking
- Exception handling
- Automatic memory management
- No pointer arithmetic

### 5. **Secure**
- No explicit pointers
- Programs run inside JVM sandbox
- Security manager for access control
- Bytecode verification

### 6. **Multithreaded**
- Built-in support for concurrent programming
- Thread class and Runnable interface
- Synchronized methods and blocks

### 7. **Architecture Neutral**
- Bytecode is platform-independent
- No implementation-dependent features

### 8. **High Performance**
- JIT (Just-In-Time) compiler
- Optimized bytecode execution
- Native method support

### 9. **Distributed**
- Rich APIs for networking (RMI, CORBA)
- Easy to create distributed applications

### 10. **Dynamic**
- Classes loaded on demand
- Supports dynamic method dispatch
- Reflection API

---

## 💻 Example 1: Hello World Program

```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

**Compilation & Execution:**
```bash
javac HelloWorld.java  # Compiles to HelloWorld.class
java HelloWorld         # Runs the program
```

**Output:**
```
Hello, World!
```

---

## 💻 Example 2: Basic Java Program Structure

```java
// Package declaration (optional)
package com.example;

// Import statements
import java.util.Scanner;

// Class declaration
public class Student {
    // Instance variables
    private String name;
    private int age;
    
    // Constructor
    public Student(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    // Methods
    public void display() {
        System.out.println("Name: " + name);
        System.out.println("Age: " + age);
    }
    
    // Main method (entry point)
    public static void main(String[] args) {
        Student student = new Student("Alice", 20);
        student.display();
    }
}
```

**Output:**
```
Name: Alice
Age: 20
```

---

## 💻 Example 3: Variables and Data Types

```java
public class DataTypesDemo {
    public static void main(String[] args) {
        // Primitive data types
        byte b = 100;                    // 8-bit
        short s = 30000;                 // 16-bit
        int i = 1000000;                 // 32-bit
        long l = 100000000000L;          // 64-bit
        
        float f = 3.14f;                 // 32-bit floating point
        double d = 3.14159265359;        // 64-bit floating point
        
        char c = 'A';                    // 16-bit Unicode
        boolean bool = true;             // true/false
        
        // Reference type
        String name = "Java Programming";
        
        // Display values
        System.out.println("byte: " + b);
        System.out.println("short: " + s);
        System.out.println("int: " + i);
        System.out.println("long: " + l);
        System.out.println("float: " + f);
        System.out.println("double: " + d);
        System.out.println("char: " + c);
        System.out.println("boolean: " + bool);
        System.out.println("String: " + name);
    }
}
```

---

## 💻 Example 4: Comments in Java

```java
public class CommentsDemo {
    public static void main(String[] args) {
        // This is a single-line comment
        System.out.println("Single-line comment above");
        
        /*
         * This is a multi-line comment
         * Used for longer explanations
         * Can span multiple lines
         */
        System.out.println("Multi-line comment above");
        
        /**
         * This is a JavaDoc comment
         * Used for generating documentation
         * @author John Doe
         * @version 1.0
         */
        System.out.println("JavaDoc comment above");
    }
}
```

---

## 💻 Example 5: Java Naming Conventions

```java
public class NamingConventionsDemo {
    // Class name: PascalCase (UpperCamelCase)
    
    // Constants: UPPER_CASE with underscores
    public static final int MAX_SIZE = 100;
    public static final String APP_NAME = "MyApp";
    
    // Variables: camelCase (lowerCamelCase)
    private int studentAge;
    private String firstName;
    
    // Methods: camelCase
    public void calculateTotal() {
        // Local variables: camelCase
        int totalAmount = 0;
        double averageScore = 95.5;
        
        System.out.println("Total: " + totalAmount);
        System.out.println("Average: " + averageScore);
    }
    
    // Package name: lowercase
    // package com.example.myapp;
}
```

---

## 💻 Example 6: Input and Output

```java
import java.util.Scanner;

public class InputOutputDemo {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        // Input
        System.out.print("Enter your name: ");
        String name = scanner.nextLine();
        
        System.out.print("Enter your age: ");
        int age = scanner.nextInt();
        
        System.out.print("Enter your GPA: ");
        double gpa = scanner.nextDouble();
        
        // Output
        System.out.println("\n--- Your Details ---");
        System.out.println("Name: " + name);
        System.out.println("Age: " + age);
        System.out.println("GPA: " + gpa);
        
        // Formatted output
        System.out.printf("Hello %s, you are %d years old with GPA %.2f\n", 
                         name, age, gpa);
        
        scanner.close();
    }
}
```

**Sample Run:**
```
Enter your name: Alice
Enter your age: 20
Enter your GPA: 3.85

--- Your Details ---
Name: Alice
Age: 20
GPA: 3.85
Hello Alice, you are 20 years old with GPA 3.85
```

---

## 💻 Example 7: Type Conversion

```java
public class TypeConversionDemo {
    public static void main(String[] args) {
        // Implicit conversion (widening)
        int intVal = 100;
        long longVal = intVal;           // int to long
        float floatVal = intVal;         // int to float
        double doubleVal = floatVal;     // float to double
        
        System.out.println("Implicit Conversion:");
        System.out.println("int: " + intVal);
        System.out.println("long: " + longVal);
        System.out.println("float: " + floatVal);
        System.out.println("double: " + doubleVal);
        
        // Explicit conversion (narrowing)
        double d = 99.99;
        int i = (int) d;                 // Truncates decimal part
        
        System.out.println("\nExplicit Conversion:");
        System.out.println("double: " + d);
        System.out.println("int: " + i);
        
        // String to primitive
        String str = "123";
        int num = Integer.parseInt(str);
        double dbl = Double.parseDouble("45.67");
        
        System.out.println("\nString to Primitive:");
        System.out.println("String: " + str + " → int: " + num);
        System.out.println("String: 45.67 → double: " + dbl);
        
        // Primitive to String
        String s1 = String.valueOf(100);
        String s2 = Integer.toString(200);
        String s3 = "" + 300;  // Concatenation
        
        System.out.println("\nPrimitive to String:");
        System.out.println(s1 + ", " + s2 + ", " + s3);
    }
}
```

---

## 📊 Java vs Other Languages

| Feature | Java | C++ | Python | JavaScript |
|---------|------|-----|--------|------------|
| **Platform** | Platform-independent | Platform-dependent | Platform-independent | Browser/Node.js |
| **Compilation** | Compiled to bytecode | Compiled to machine code | Interpreted | Interpreted/JIT |
| **Memory** | Automatic (GC) | Manual | Automatic (GC) | Automatic (GC) |
| **Pointers** | No explicit pointers | Yes | No | No |
| **OOP** | Fully OOP | Multi-paradigm | Multi-paradigm | Multi-paradigm |
| **Typing** | Static, Strong | Static, Strong | Dynamic, Strong | Dynamic, Weak |
| **Speed** | Fast | Fastest | Slower | Fast (JIT) |
| **Use Cases** | Enterprise, Android | System programming | Data Science, AI | Web development |

---

## 📊 Java Editions

### 1. **Java SE (Standard Edition)**
- Core Java platform
- Basic libraries (Collections, I/O, Networking)
- Desktop applications

### 2. **Java EE (Enterprise Edition)**
- Enterprise applications
- Servlets, JSP, EJB, JMS
- Web services, REST APIs

### 3. **Java ME (Micro Edition)**
- Mobile and embedded devices
- IoT applications
- Limited resources

### 4. **JavaFX**
- Rich GUI applications
- Multimedia support
- Modern UI controls

---

## 🎯 Java Applications

### 1. **Enterprise Applications**
- Banking systems
- E-commerce platforms
- ERP systems

### 2. **Mobile Applications**
- Android apps
- Cross-platform mobile apps

### 3. **Web Applications**
- Spring Boot
- JSP/Servlets
- RESTful services

### 4. **Desktop Applications**
- JavaFX applications
- Swing applications

### 5. **Big Data**
- Hadoop ecosystem
- Apache Spark
- Kafka

### 6. **Cloud Applications**
- Microservices
- AWS, Azure, GCP

---

## 💡 Best Practices

1. ✅ Follow naming conventions
2. ✅ Write meaningful comments
3. ✅ Use proper indentation
4. ✅ Keep methods small and focused
5. ✅ Handle exceptions properly
6. ✅ Close resources (Scanner, files, connections)
7. ✅ Use meaningful variable names
8. ✅ Avoid magic numbers (use constants)

---

## 🎯 Interview Questions

1. **What is Java?**
2. **What are the main features of Java?**
3. **What is platform independence?**
4. **Explain WORA (Write Once, Run Anywhere)**
5. **What are the differences between Java and C++?**
6. **What is bytecode?**
7. **Why is Java not 100% object-oriented?**
8. **What are the Java editions?**
9. **What is the difference between JDK, JRE, and JVM?**
10. **What are the naming conventions in Java?**

---

## 📚 Next Topics

- [JDK, JRE, JVM Architecture](02.%20JDK%2C%20JRE%2C%20JVM%20Architecture.md)
- [Data Types & Variables](03.%20Data%20Types%20%26%20Variables.md)
- [Operators & Expressions](04.%20Operators%20%26%20Expressions.md)
